(ns fatwheel.core-test
  (:require [clojure.test :refer :all]
            [fatwheel.core :refer :all]))

(deftest a-test
  (testing "Intentionally failing to demonstrate behavior (TODO: remove me later)."
    (is (= 0 1))))
